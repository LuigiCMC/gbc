import React from "react";
import Image from "next/image";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

const properties = [
  {
    id: 1,
    title: "Apartamento en Barranquilla",
    price: "$500,000 COP/noche",
    image: "/property1.jpg",
  },
  {
    id: 2,
    title: "Casa en Bogotá",
    price: "$750,000 COP/noche",
    image: "/property2.jpg",
  },
];

export default function Home() {
  return (
    <div className="container mx-auto p-6 bg-black text-white">
      <header className="bg-yellow-500 p-4 flex justify-between items-center">
        <h1 className="text-3xl font-bold">Samba Real Estate</h1>
        <nav>
          <ul className="flex space-x-6">
            <li><a href="#" className="hover:underline">Qué Hacemos</a></li>
            <li><a href="#" className="hover:underline">Tarifas</a></li>
            <li><a href="#" className="hover:underline">Quiénes Somos</a></li>
            <li><a href="#propiedades" className="hover:underline">Propiedades</a></li>
          </ul>
        </nav>
      </header>
      
      <section className="text-center py-16">
        <h1 className="text-5xl font-bold">Empieza hoy mismo</h1>
        <p className="mt-4 text-lg">Vente con nosotros y aumenta tus ingresos</p>
        <div className="mt-6">
          <Button className="bg-white text-black px-6 py-3 mr-4">Cómo Funciona</Button>
          <Button className="bg-yellow-500 text-black px-6 py-3">Empieza Ya</Button>
        </div>
      </section>

      <section id="propiedades" className="py-16">
        <h2 className="text-4xl font-bold text-center mb-8">Propiedades</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {properties.map((property) => (
            <Card key={property.id} className="rounded-2xl shadow-lg overflow-hidden">
              <Image
                src={property.image}
                alt={property.title}
                width={400}
                height={250}
                className="w-full h-60 object-cover"
              />
              <CardContent className="p-4">
                <h2 className="text-xl font-semibold">{property.title}</h2>
                <p className="text-gray-400">{property.price}</p>
                <Button className="mt-4 w-full bg-yellow-500 text-black">Ver Detalles</Button>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>
      
      <footer className="bg-yellow-500 p-4 text-center">
        <p>Escríbenos al WhatsApp</p>
      </footer>
    </div>
  );
}
